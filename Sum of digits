#include<iostream>
using namespace std;
int main()
{
    int number,total=0;
    cout<<"Enter a number:";
    cin>>number;
    while(number!=0)
    {
        int digit=number%10;
        number/=10;
        total += digit;
        digit=0;
    }
    cout<<total;
}
