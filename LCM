#include<iostream>
using namespace std;
int main()
{
    int a,b,lcm;
    cout<<"Enter the number a and b:";
    cin>>a>>b;
    lcm=max(a,b);
    while(true)
    {
        if(lcm%a==0 && lcm%b==0)
        {
            break;
        }
        lcm++;
    }
    cout<<lcm;
}
